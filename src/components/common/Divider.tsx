export interface DividerProps {
    margin?: string
    marginX?: string,
    marginY?: string,
}

export default function Divider() {
    return (
        <div className={"divide-y"}>

        </div>
    );
}